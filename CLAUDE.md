# פרויקט: קורס "בק-אופיס עם AI" — מוצר דיגיטלי למכירה

## מה זה הריפו הזה
קורס דיגיטלי בעברית (RTL) למנהלות/י אדמיניסטרציה: "איך להפוך ניהול אדמיניסטרטיבי לשירות".
הבעלים: adielbaz14 (פנייה בלשון נקבה). המטרה העסקית: מכירת הקורס כמוצר דיגיטלי.

## מבנה הריפו
- `README.md` — עמוד הבית של הקורס עם ניווט
- `course/00..09-*.md` — 10 מודולי הקורס (סילבוס → פרויקט מסכם)
- `course/templates/` — נספחים: ספריית 25 פרומפטים + תבנית קטלוג שירותים/SLA
- `dist/` — קבצי המוצר למכירה: HTML יחיד מעוצב + PDF (נוצרו מסקריפט python+chromium headless)
- `marketing/sales-page.md` — טיוטת דף מכירה (Payhip/Gumroad) עם הערות תמחור פנימיות

## סטטוס נכון לעכשיו (יולי 2026)
✅ הקורס המלא כתוב, נבדק (קישורים פנימיים, הגהה) ומוזג ל-main (PR #1, PR #2)
✅ קבצי מוצר: dist/back-office-ai-course.{html,pdf} — שער, תוכן עניינים, עיצוב RTL מלא
✅ נשלחה למשתמשת חבילת ZIP למכירה (PDF + HTML + קרא-אותי) + טיוטת דף מכירה
✅ הוחלט על אסטרטגיית מכירה: Payhip (PDF stamping + הגבלת הורדות), מכירת שני הפורמטים כחבילה אחת

## הצעדים הבאים (מכאן ממשיכים)
1. המשתמשת מקימה חשבון Payhip ומעלה את ה-ZIP (הנחיות ניתנו בשיחה הקודמת)
2. ליטוש דף המכירה ב-marketing/sales-page.md והתאמתו לפלטפורמה שנבחרה
3. אפשרויות שהוצעו וטרם בוצעו:
   - ליד-מגנט: גרסת "טעימה" חינמית (מודול 1 בלבד) לאיסוף מיילים
   - עמוד נחיתה שיווקי מלא
   - מיתוג אישי על השער (שם, לוגו, צבעים)
   - בהמשך: מעבר לפלטפורמת קורסים עם לוגין (Podia/סקולר) אם יש ביקוש

## הערות טכניות לבנייה מחדש של המוצר
- סקריפט הבנייה נמצא בהיסטוריית השיחה (build_course.py): ממיר את כל ה-md לקובץ HTML יחיד
  (python-markdown, extensions: tables/fenced_code/sane_lists), ממפה קישורי .md לעוגנים,
  מסיר ניווט הקודם/הבא, וממיר צ'קבוקסים ל-☐
- PDF: chromium headless (`/opt/pw-browsers/chromium --headless --print-to-pdf --no-pdf-header-footer`)
- דרוש פונט עברי: `apt-get install fonts-noto-core` (Noto Sans Hebrew)
- עדכון תוכן: לערוך את קבצי ה-md, להריץ את הסקריפט מחדש, ולהחליף את הקבצים ב-dist/

## כללי עבודה בריפו
- ענף עבודה: claude/back-office-ai-course-ys4t3e (מתאפס מ-main אחרי כל מיזוג)
- המשתמשת מאשרת מיזוגים בקלות — לפתוח PR ולמזג כשהעבודה שלמה
- כל התוכן בעברית; לשמור על עקביות מבנה המודולים (מטרות → תוכן → תרגיל → צ'קליסט)
